apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: custom-config
spec:
  version: v1
  httpPipeline:
    handlers:
      - name: uppercase
        type: middleware.http.uppercase
      - name: azuread-auth
        type: middleware.http.bearer
